import React from 'react';

import { paginationBar } from "./Pagination.css";

export const Pagination: React.FunctionComponent = () => {
  return (
    <div className={paginationBar}>
      test
    </div>
  )
}

export default Pagination;
